# Railway configuration for Ponder Indexer service

[build]
builder = "dockerfile"
dockerfilePath = "../Dockerfile.indexer"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
port = 42069